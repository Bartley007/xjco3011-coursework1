version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./books.db:/app/books.db
    environment:
      - DATABASE_URL=sqlite:///./books.db
    restart: unless-stopped
